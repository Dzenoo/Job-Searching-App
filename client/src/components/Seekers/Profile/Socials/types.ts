type SocialsProps = {
  seeker: Seeker;
};

type SocialsDialogProps = {
  seeker: {
    portfolio: string;
    linkedin: string;
    github: string;
  };
};

export { type SocialsProps, type SocialsDialogProps };
