type SocialsProps = {
  seeker: Seeker;
};

type SocialsDialogProps = {
  seeker: {
    linkedin: string;
    github: string;
    portfolio: string;
  };
};

export { type SocialsProps, type SocialsDialogProps };
